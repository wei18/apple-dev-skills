Official pages backing this skill's claims; read when verifying or updating a factual or version-sensitive claim.

| Page | URL | Backs |
|---|---|---|
| Configure permissions | https://code.claude.com/docs/en/permissions#bash | Dispatch contract item 2 "Forbidden tools": a layered CLI ban can be enforced with Bash deny rules (which apply to subagents); the rule stays in prose where no rule is configured |
| Create custom subagents | https://code.claude.com/docs/en/sub-agents#available-tools | Same item, caveat: `disallowedTools: Bash(...)` removes the whole Bash tool, including the allowed read-only `git log` / `git show` |
| Git - git-diff Documentation | https://git-scm.com/docs/git-diff | references/phase-todo-sweep.md: `<base>...HEAD` (from merge-base) vs `..` (two tips); `--diff-filter=d` (lowercase excludes deleted); `-z` |
